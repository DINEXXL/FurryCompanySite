# 🐾 FurryCompany

Твоє справжнє «Я» заслуговує на найкраще втілення. **FurryCompany** — твій шлях до самовираження.

## 🚀 Про проєкт

Це командний навчальний проєкт, реалізований на фреймворку **Flask**. Сайт містить інформацію про послуги, портфоліо, команду та форму зворотного зв'язку.

### 🛠 Технологічний стек:

* **Python 3.13+**
* **Flask** (Backend)
* **HTML5 / CSS3** (Frontend)

---

## 📂 Структура проєкту

Команда дотримується єдиної структури файлів:

```text
/
├── static/              # Статичні файли
│   ├── css/
│   │   └── style.css    # Загальні стилі проєкту
│   └── img/             # Зображення та favicon
├── templates/           # HTML шаблони (Jinja2)
│   ├── base.html        # Базовий шаблон (шапка, підвал)
│   ├── home.html        # Головна
│   ├── about.html       # Про нас
│   ├── services.html    # Послуги
│   ├── portfolio.html   # Портфоліо
│   ├── contacts.html    # Контакти
│   └── 404.html         # Сторінка помилки
├── app.py               # Точка входу Flask
├── requirements.txt     # Список залежностей
└── README.md            # Документація

```

---

## 🗺 Маршрути (Routes) та Схема

| Шлях (`Path`) | Функція | Опис |
| --- | --- | --- |
| `/` | `home()` | Головна сторінка з hero-блоком та анонсами. |
| `/about` | `about()` | Історія компанії, місія та картки команди. |
| `/services` | `services()` | Перелік послуг (Веб-девелопмент, SEO, Дизайн тощо). |
| `/portfolio` | `portfolio()` | 6 реальних/фейкових кейсів з описом технологій. |
| `/contacts` | `contacts()` | Форма зв'язку (GET/POST) з валідацією. |
| `any` | `page_not_found()` | Обробка помилки 404. |

---

## 🔧 Інструкція із запуску

### 1. Підготовка середовища

Переконайтеся, що у вас встановлено Python версії 3.13.

### 2. Клонування репозиторію та встановлення залежностей

```bash
git clone <url_вашого_репозиторію>
cd FurryCompany
pip install -r requirements.txt

```

### 3. Запуск проєкту

```bash
# Для розробки (Debug mode)
python app.py

```

Сайт буде доступний за адресою: `http://127.0.0.1:5000/`

---

## 🦊 Дані компанії

* **Назва:** FurryCompany
* **Слоган:** "Твоє справжнє «Я» заслуговує на найкраще втілення. FurryCompany — твій шлях до самовираження."
* **Email:** furryCompanySupport@furaffinity.net
* **Телефон:** +380 (67) 451 61 67
* **Адреса:** м. Жидомир, вул. Бойкіссера, 69

---

*FurryCompany 2026. Made with ❤️ and Flask.*
